[project]
name = "flexgrid"
version = "0.0.1"
dependencies = [
    "speechbrain",
]

[tool.black]
line-length = 80

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["flexgrid*"]